import 'package:flutter/material.dart';

class PointReviewTab extends StatelessWidget {
  final String token;
  const PointReviewTab({super.key, required this.token});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Point Review Tab'),
    );
  }
}
